# go-go-hooligan 音声実装構成

## 目的
フーリガンの世界観に合う効果音・音声を追加し、ゲーム体験を向上させる。

## 実装方針
- 最小構成で実装（後で拡張可能）
- audio フォルダで一元管理
- 再生箇所を明確に整理
- 多重再生制御
- 音量バランス調整
- モバイル対応

## フォルダ構造

```
go-go-hooligan/
├── audio/
│   ├── se/                    # 効果音
│   │   ├── title.mp3          # タイトルSE
│   │   ├── button.mp3         # ボタン押下音
│   │   ├── recruit-success.mp3 # 勧誘成功音
│   │   └── attack.mp3         # 最終決戦の攻撃音
│   ├── voice/                 # ボイス
│   │   ├── victory.mp3        # 勝利ボイス
│   │   └── defeat.mp3         # 敗北ボイス
│   └── README.md              # 音声ファイル管理
├── audio-manager.js           # AudioManager クラス
└── game.js                    # メインゲームファイル
```

## 音声ファイル仕様

| ファイル | タイプ | 用途 | 長さ | 形式 | 音量 |
|---------|--------|------|------|------|------|
| title.mp3 | SE | タイトル画面表示時 | 1-2秒 | MP3 | -6dB |
| button.mp3 | SE | ボタン押下時 | 0.3-0.5秒 | MP3 | -12dB |
| recruit-success.mp3 | SE | 勧誘成功時 | 1-2秒 | MP3 | -9dB |
| attack.mp3 | SE | 最終決戦の攻撃時 | 0.5-1秒 | MP3 | -6dB |
| victory.mp3 | Voice | 勝利時 | 2-3秒 | MP3 | -3dB |
| defeat.mp3 | Voice | 敗北時 | 2-3秒 | MP3 | -3dB |

## AudioManager クラス設計

### 機能
- 音声ファイルの再生
- 多重再生の制御（同時再生しない）
- 音量制御
- モバイル対応

### メソッド
```javascript
class AudioManager {
  // 初期化
  constructor()
  
  // 効果音再生
  playSE(name)          // SE再生（多重再生なし）
  playVoice(name)       // ボイス再生（多重再生なし）
  
  // 制御
  stop(type)            // 停止（'se' | 'voice' | 'all'）
  setVolume(type, vol)  // 音量設定（0-1）
  
  // ユーティリティ
  isPlaying(type)       // 再生中か判定
}
```

### 実装ポイント
- `HTMLAudioElement` を使用
- 再生中は再度再生しない
- モバイルでは最初のユーザー操作後に再生可能
- 音量は dB 値で管理

## 再生箇所

### タイトルSE
- **箇所**: `renderTitleScreen()` 内
- **タイミング**: 画面表示時（1回のみ）
- **制御**: タイトル画面遷移時に停止

### ボタン押下音
- **箇所**: `browser_click` イベント時
- **タイミング**: ボタンクリック時
- **制御**: 多重再生なし

### 勧誘成功音
- **箇所**: `processRecruit()` 内
- **タイミング**: 勧誘成功確定時
- **制御**: 1回のみ

### 最終決戦の攻撃音
- **箇所**: `resolveBattle()` 内
- **タイミング**: 攻撃実行時
- **制御**: 多重再生なし

### 勝利ボイス
- **箇所**: `resolveBattle()` 内（勝利判定時）
- **タイミング**: 勝利確定時
- **制御**: 1回のみ

### 敗北ボイス
- **箇所**: `resolveBattle()` 内（敗北判定時）
- **タイミング**: 敗北確定時
- **制御**: 1回のみ

## 音量バランス

### 基準
- 全体: -12dB（モバイルスピーカー基準）
- ボイス: -3dB（目立たせる）
- 攻撃音: -6dB（迫力重視）
- ボタン音: -12dB（邪魔しない）

### 調整方法
```javascript
audioManager.setVolume('se', 0.5)      // SE: 50%
audioManager.setVolume('voice', 0.8)   // Voice: 80%
```

## モバイル対応

### 制約
- iOS Safari: ユーザー操作後のみ再生可能
- Android: 通常再生可能

### 対応方法
- 最初のボタンクリック時に `AudioContext` を初期化
- 再生失敗時は無視（エラー処理）

## 実装ステップ

1. **audio フォルダ作成**
   ```bash
   mkdir -p audio/se audio/voice
   ```

2. **AudioManager クラス作成**
   - `audio-manager.js` を作成
   - 再生、停止、音量制御を実装

3. **game.js に統合**
   - `AudioManager` をインスタンス化
   - 各箇所に再生コードを追加

4. **音量バランス調整**
   - 各音声ファイルのレベルを確認
   - 必要に応じて dB 値を調整

5. **テスト**
   - デスクトップで動作確認
   - モバイルで動作確認
   - 音量バランス確認

## 今後の拡張

- BGM 追加（タイトル、戦闘、勝利など）
- 音声ファイルの圧縮（ファイルサイズ削減）
- 音量設定画面の追加
- 効果音の種類追加（ダメージ音、回復音など）
- 音声言語の切り替え

## 注意事項

- 音声ファイルは著作権フリーまたは商用利用可能なものを使用
- ファイルサイズは 1MB 以下を目安に
- MP3 形式で統一（ブラウザ互換性）
- モバイルでの再生テストは必須
